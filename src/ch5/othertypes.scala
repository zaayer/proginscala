// a symbol literal is written with a leading single quote
// typically used where you would use just an identifier in
// a dynamically typed language

val s = 'aSymbol
val nm = s.name

println(nm)

// booleans are true and false, no capitalization

val bool = true
val fool = false
